const ADD_SITEBAR = "ADD-SITEBAR";
const UPDATE_NEW_SITEBAR = "UPDATE-NEW-SITEBAR";

let initialState = {
  sitebar: [],
};
const sitebarReducer = (state = initialState, action) => {
  return state;
};

export default sitebarReducer;
